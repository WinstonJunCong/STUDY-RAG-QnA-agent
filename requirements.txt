# torch must be installed manually with CUDA support:
# pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# plain `pip install torch` will pull CPU-only build from PyPI
torch==2.6.0

# Core framework
llama-index>=0.10.0
llama-index-vector-stores-chroma>=0.1.0

# BM25 + Reciprocal Rank Fusion
llama-index-retrievers-bm25>=0.1.0
rank-bm25>=0.2.0

# Free local embeddings (no API key needed)
llama-index-embeddings-huggingface>=0.2.0
sentence-transformers>=2.7.0

# Reranker (second-pass relevance scoring)
llama-index-postprocessor-flag-embedding-reranker>=0.1.0

# Free local LLM via Ollama
llama-index-llms-ollama>=0.1.0

# Vector store (local, no API key)
chromadb>=0.5.0

# Document loaders
beautifulsoup4>=4.12.0
requests>=2.31.0

# Video transcription (free, local)
openai-whisper>=20231117

# Utilities
python-dotenv>=1.0.0
rich>=13.0.0        # pretty terminal output
psutil>=5.9.0       # system RAM monitoring (for diagnostics)

# Unstructured.io - document parsing (markdown, PDF, HTML, DOCX, PPTX)
unstructured>=0.10.0
unstructured[md]>=0.10.0

# API Server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
aiosqlite>=0.19.0   # async SQLite for query logging
numpy>=1.24.0       # for semantic similarity in cache
python-multipart>=0.0.6  # for file upload forms

# Database
psycopg2-binary>=2.9.0  # PostgreSQL support
